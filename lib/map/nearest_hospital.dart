import 'package:flutter/material.dart';

class NearestHospital extends StatelessWidget {
  static const String routeName = ' NearestHospital ';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Nearest Hospital')),
    );
  }
}
