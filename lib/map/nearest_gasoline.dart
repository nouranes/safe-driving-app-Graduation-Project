import 'package:flutter/material.dart';

class NearestGasoline extends StatelessWidget {
  static const String routeName = ' NearestGasoline';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Nearest Gasoline')),
    );
  }
}
